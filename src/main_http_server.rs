use rust_git::server_http::web_server::WebServer;

fn main() -> Result<(), std::io::Error> {
    //match WebServer::new() {
    //    Ok(_) => println!("La función web server se ejecutó correctamente."),
    //    Err(_) => println!("Hubo un error al ejecutar la función web server."),
    //};

    Ok(())
}
